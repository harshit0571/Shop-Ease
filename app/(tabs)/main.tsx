import { View, Text } from "react-native";
import React from "react";
import { Link } from "expo-router";
import { SafeAreaView } from "react-native-safe-area-context";

const main = () => {
  return (
    <SafeAreaView>
      <Text>maijhcjbdjbjbbdfjbjfbn</Text>
      <Link href={'/(products)'}>j</Link>
    </SafeAreaView>
  );
};

export default main;
